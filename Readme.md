# 🐍 Classic Snake Game

A simple implementation of the classic Snake game built using Python's built-in `turtle` graphics library.

## 🎮 Features
* Real-time score tracking.
* Collision detection (walls and tail).
* Simple and responsive controls.

## ⚙️ Requirements
* Python 3.x
* No external libraries needed (uses standard `turtle`,  `random`).

## 🚀 How to Run
1. Clone or download this repository.
2. Open your terminal/command prompt.
3. Run the following command:
   ```bash
   python main.py